@*@model SCMS.Models.ViewModels.HomeVM

@{
    ViewBag.Title = "CategoryList";
}

<h2>Category List</h2>

@using (Html.BeginForm("CategoryList", "Homepage", FormMethod.Post))
{
		for (int i = 0; i < Model.CategoryCheckboxes.Count; i++)
		{
			<p>
				@Html.CheckBoxFor(m => m.CategoryCheckboxes[i].isSelected)
				<span>@Model.CategoryCheckboxes[i].Category.Description</span>
				@Html.HiddenFor(m => m.CategoryCheckboxes[i].Category.CategoryId)
			</p>
		}
			<input type="submit" value="Submit Form"/>*@
}